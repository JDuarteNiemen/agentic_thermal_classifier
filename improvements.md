

# Summary classification
Increase the number of PMIDS obtained from query search. Run them through regex scan and only keep papers that hit for
keywords

Add a pmc search

Change how papers are deemed as relevant

Could potentially only pass papers to FilterRelevant node if they hit for at least one of the regex tests
